[pytest]
testpaths = tests
markers =
    slow: hits a live model or the network; minutes, not milliseconds

# Unit tests run anywhere:
#   pytest -m "not slow"
# Full suite needs the backend up on :8001 with EMERGENT_LLM_KEY configured:
#   pytest
